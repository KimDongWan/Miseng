#pragma strict

var target : GUITexture;
static var pos_x = 0.0;
static var pos_y = 0.0;

/*function OnMouseDown() {
	var pos = Input.mousePosition;
	//pos.x = (pos.x - 1190)*0.007 + 990;
	//pos.y = (pos.y - 110)*0.007 + 110;
	pos.x = pos.x/Screen.width;
	pos.y = pos.y/Screen.height;
	//pos.x /= 904;
	//pos.y /= 427;
	target.transform.position = pos;
}*/

function Update() {
	//target.transform.localPosition.x = pos_x;
	//target.transform.localPosition.y = pos_y;
}